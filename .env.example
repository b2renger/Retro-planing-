# Optional server-side Gemini key used by the dev/hosted Express server (never required for the desktop app:
# providers are configured per user in Settings > AI Providers).
GEMINI_API_KEY=
# Port for the dev/hosted server
PORT=3000
